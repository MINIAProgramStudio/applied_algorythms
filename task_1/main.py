from Set import Set
from random import random

class UncleFester_SkilledTester:
    def __init__(self):
        pass

    def random_set(length, minimum, maximum, integer = False):
        set = Set()
        for i in range(length):
            set.insert(random() * multiply_by + offset)
        return set

    def random_int_set(self, range, minimum):
        set = Set()
        for i in range(length):
            set.insert(random() * multiply_by + offset)
        return set





